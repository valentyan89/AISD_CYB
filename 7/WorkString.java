public interface WorkString {
    int countChars(String s);// s - start string, return len of the string
    String newString(String s);// s - start string, return new string just with indexes % 2 == 1
    String reverseString(String s);// s - start string, return reversed string
}
